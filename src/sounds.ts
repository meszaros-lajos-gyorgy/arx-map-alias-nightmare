import { Audio } from 'arx-level-generator'

export const uruLink = Audio.fromCustomFile({
  filename: 'uru_link.wav',
  sourcePath: './sounds',
})

export const thunderstorm = Audio.fromCustomFile({
  filename: 'thunderstorm.wav',
  sourcePath: './sounds',
})

export const musicBySirs = Audio.fromCustomFile({
  filename: 'music_by_sirs.wav',
})
